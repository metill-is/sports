# R
.Rproj.user
.Rhistory
.RData
.Ruserdata

# Environment
.Renviron

# Derived artefacts (regenerable from Parquet)
sports.duckdb
sports.duckdb.wal

# Testing/research
research/results/
.testthat/

# Editor
.DS_Store
*.swp

# Temporary
*.tmp
_legacy-tmp/
sports-backup-*/

# Stan build artefacts
*.exe
*.hpp
**/*.stan.d
Stan/*/*
!Stan/*/*.stan

# Personal Claude Code preferences (not shared)
.claude/settings.local.json

# Subagent worktrees (ephemeral)
.claude/worktrees/

# Scheduled-task runtime lock
.claude/scheduled_tasks.lock

# Stan fit objects (10MB+ each, regenerable from data/facts/)
data/beliefs/fits/
data/beliefs/fits_by_round/
.worktrees/

# Quarto report renders (regenerable from .qmd; can be multi-MB self-contained HTML)
docs/reports/*.html
docs/reports/*_files/

# Isolated "what-if" replay tree (data/publish_replay/2026-05-15/, etc.)
# — produced by scripts/0Nr_replay.R --publish-to, never meant to ship
data/publish_replay/

# Backtest artefacts (regenerable via scripts/0Nb_backtest.R)
data/backtest/
