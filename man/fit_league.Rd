% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/model-league.R
\name{fit_league}
\alias{fit_league}
\title{End-to-end: prepare data, fit Stan, extract posteriors, write beliefs.}
\usage{
fit_league(
  league_key = NULL,
  league = NULL,
  sex,
  fit_date = Sys.Date(),
  end_date = fit_date,
  root = here::here("data"),
  stan_dir = here::here("Stan"),
  method = "sample",
  iter_warmup = 1000L,
  iter_sampling = 1000L,
  chains = 4L,
  seed = NULL,
  from_season = NULL,
  schedule_horizon_days = 14L,
  write_archive = TRUE
)
}
\arguments{
\item{league_key}{Key into `load_leagues()`. Mutually exclusive with `league`.}

\item{league}{Pre-loaded league list. Mutually exclusive with `league_key`.}

\item{sex}{"male" or "female".}

\item{fit_date}{Date stamped on every posterior row. Default today.}

\item{end_date}{Training cutoff. Default = `fit_date`.}

\item{root}{Data root. Default `here::here("data")`.}

\item{stan_dir}{Stan-model root. Default `here::here("Stan")`.}

\item{method}{Passed to `fit_model()`. "sample" (default), "pathfinder", or "variational".}

\item{iter_warmup, iter_sampling}{MCMC iteration counts. Passed to `fit_model()`.}

\item{chains}{Number of MCMC chains. Passed to `fit_model()`.}

\item{seed}{Integer seed for reproducibility. NULL = cmdstanr default.}

\item{from_season}{Optional integer: earliest season to include in training data.}

\item{schedule_horizon_days}{Days ahead of `end_date` to include from schedule. Default 14.}

\item{write_archive}{Write `beliefs/archive/` in addition to `beliefs/latest/`? Default TRUE.}
}
\value{
Tibble of beliefs (invisibly).
}
\description{
Supports two call modes:
  1. By league_key: `fit_league("basketball_iceland", sex = "male")`
     -- looks up the league via `load_leagues()`.
  2. By league list: `fit_league(league = <list>, sex = "male")`
     -- bypasses `load_leagues()`; used by tests and one-off runs.
}
\details{
Writes `data/beliefs/latest/` (snapshot -- overwritten per call) and
optionally `data/beliefs/archive/sport=X/country=Y/sex=Z/fit_date=D/`.
}
