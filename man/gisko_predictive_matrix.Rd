% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/gisko.R
\name{gisko_predictive_matrix}
\alias{gisko_predictive_matrix}
\title{Integrated posterior-predictive scoreline matrix for a pairing}
\usage{
gisko_predictive_matrix(
  home,
  away,
  sim_inputs,
  venue = "neutral",
  max_goals = 8L
)
}
\arguments{
\item{home, away}{Team names (martj42 convention, as in `sim_inputs`).}

\item{sim_inputs}{`list(team=, scalar=)` from `data/wc/fit/sim_inputs.rds`.}

\item{venue}{One of "neutral" (all knockouts), "home", "away".}

\item{max_goals}{Grid upper bound.}
}
\value{
list `pbar`, `lambdas` (`n_draws x 3`), `n_draws`.
}
\description{
Reuses the WC model's rate function `.wc_match_lambdas()` (single source of
truth) so this can never drift from the forecast. Builds the analytic
bivariate-Poisson PMF per posterior draw and averages.
}
