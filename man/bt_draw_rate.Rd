% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/backtest-pit.R
\name{bt_draw_rate}
\alias{bt_draw_rate}
\title{Predicted vs observed draw rate, with the (observed - predicted) gap.}
\usage{
bt_draw_rate(predicted, results, by = "sex")
}
\arguments{
\item{predicted}{Output of [bt_load_predicted()].}

\item{results}{Results store.}

\item{by}{Optional grouping columns (e.g. `c("sex", "division")`).}
}
\value{
`(<by..>, n, predicted_draw_rate, observed_draw_rate, gap)`.
}
\description{
Predicted draw probability per match = as-of `P(home_goals == away_goals)`
from the draw counts; observed = the realised draw indicator. A persistent
positive gap (model under-predicts draws) is the canonical signal for a
Dixon-Coles low-score correction or a bivariate-Poisson correlation term.
}
