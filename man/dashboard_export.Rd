% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/dashboard-export.R
\name{dashboard_export}
\alias{dashboard_export}
\title{Run the diagnostics for real and write the dashboard JSON contract.}
\usage{
dashboard_export(
  root = here::here("data"),
  out_dir = here::here("data", "dashboard"),
  season = 2026L,
  now = Sys.time(),
  wf_fn = function(s) bt_walkforward_reuse(s, season = season, root = root)
)
}
\arguments{
\item{root}{Data root.}

\item{out_dir}{Output directory for the JSON contract.}

\item{season}{Season year.}

\item{now}{Generation timestamp.}

\item{wf_fn}{Walk-forward function `(sex) -> list(bets=...)` or NULL.}
}
\value{
The assembled contract, invisibly.
}
\description{
Loads `results`/`odds`/predicted extracts, runs the REUSE-mode walk-forward
per sex (`wf_fn`, injectable for tests), attaches division, assembles the
contract, and writes it. Strictly read-only on the ledger.
}
