% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/wc-accountability.R
\name{wc_backfill_snapshots}
\alias{wc_backfill_snapshots}
\title{Backfill the accountability snapshot log from the git history of
`predictions.json`.}
\usage{
wc_backfill_snapshots(
  root = here::here("data"),
  repo = here::here(),
  ref = "HEAD",
  predictions_path = "data/publish/world_cup/karla/predictions.json"
)
}
\arguments{
\item{root}{Data root (where the snapshot log is written).}

\item{repo}{Repository root for git operations.}

\item{ref}{Git ref to walk (default `"HEAD"`).}

\item{predictions_path}{Repo-relative path to the published predictions.json.}
}
\value{
Invisibly, the number of historical commits folded in.
}
\description{
Every daily fit committed a `predictions.json` covering the then-upcoming
fixtures. Walking those commits oldest -> newest and upserting each into the
snapshot log reconstructs exactly what [wc_snapshot_predictions()] would have
accumulated day by day — recovering the pre-match prediction for every
fixture ever published, including matches played before this layer shipped.
Historical files carry only `goal_diff_distribution`, so the home/away/total
distributions backfill as absent and accrue from deployment forward.
Idempotent.
}
