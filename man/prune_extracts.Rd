% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/extracts-retention.R
\name{prune_extracts}
\alias{prune_extracts}
\title{Prune old `fit_date=` partitions from an extracts tree.}
\usage{
prune_extracts(
  root = here::here("data", "beliefs", "extracts"),
  keep_days = 14L,
  keep_min = 3L,
  keep_season_anchor = TRUE,
  now = Sys.Date(),
  dry_run = TRUE
)
}
\arguments{
\item{root}{Extracts root (`data/beliefs/extracts`).}

\item{keep_days}{Keep every partition whose `fit_date` is within this many
days of `now`.}

\item{keep_min}{Additionally keep this many newest partitions per
(sport, country, sex) cell REGARDLESS of age. Must be >= 1: publish reads
the newest, so dropping to zero would break a dormant cell.}

\item{now}{Reference date.}

\item{dry_run}{When `TRUE` (default) nothing is deleted; the caller gets the
list of partitions that would go.}
}
\value{
Tibble of pruned (or prunable) partitions: `sport`, `country`,
  `sex`, `fit_date`, `path`, `bytes`.
}
\description{
Prune old `fit_date=` partitions from an extracts tree.
}
