% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/placer-validate.R
\name{validate_betting_enabled}
\alias{validate_betting_enabled}
\title{Abort if any recommendation belongs to a betting-disabled league.}
\usage{
validate_betting_enabled(leagues, recs)
}
\arguments{
\item{leagues}{Named league config from [load_leagues()].}

\item{recs}{Recommendation rows about to be placed.}
}
\value{
`invisible(TRUE)`, or stops naming every offending league.
}
\description{
Placer pre-flight, mirroring [validate_team_names_config()]: runs before
the browser is launched so a policy breach fails fast and loudly rather
than part-way through a placement run. Decision D2 (spec 2026-09-02
section 3).
}
\details{
`load_recommendations()` already drops these rows, so reaching this error
means a caller bypassed the loader -- which is exactly when a loud abort
is wanted rather than a silent filter.
}
