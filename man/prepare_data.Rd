% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/model-prepare.R
\name{prepare_data}
\alias{prepare_data}
\title{Build stan_data + pred_d + teams from the facts store.}
\usage{
prepare_data(
  league,
  sex,
  end_date = Sys.Date(),
  root = here::here("data"),
  from_season = NULL,
  schedule_horizon_days = 14L
)
}
\arguments{
\item{league}{A single entry from `load_leagues()` (must have `sport` +
`country` set; `stan_model` is not read here).}

\item{sex}{"male" or "female".}

\item{end_date}{Cutoff date -- matches on or before this go into training.}

\item{root}{Data root. Default `here::here("data")`.}

\item{from_season}{Optional integer. If supplied, drop matches with
`season < from_season`.}

\item{schedule_horizon_days}{How far ahead to look for prediction targets.
Matches whose match_date falls in
`[end_date, end_date + schedule_horizon_days]` are kept.}
}
\value{
`list(stan_data, pred_d, teams)`.
}
\description{
Reads `data/facts/results/` and `data/facts/schedules/` for the given
(league, sex), assembles the canonical Stan input list, and returns it
along with the prediction tibble (for posterior-draw joining) and the
team registry. Pure function -- no file I/O beyond read_table().
}
\details{
The returned stan_data is a superset covering every field consumed by
the three production models (basketball scalar-sigma, handball per-team
sigma, football BVP). Each Stan model's data{} block picks only the
fields it declares.
}
