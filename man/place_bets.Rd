% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/placer-pipeline.R
\name{place_bets}
\alias{place_bets}
\title{Place bets from data/decisions/recommendations/.}
\usage{
place_bets(
  leagues = NULL,
  dry_run = TRUE,
  interactive = TRUE,
  today_only = FALSE,
  target_date = NULL,
  headless = TRUE,
  root = here::here("data"),
  dual_write_csv = TRUE
)
}
\arguments{
\item{leagues}{Optional character vector of league keys to restrict to
(e.g. \code{"football_iceland"}). NULL = all.}

\item{dry_run}{If \code{TRUE}, navigate and select odds but do not click
"Kaupa". Default \code{TRUE}.}

\item{interactive}{If \code{TRUE}, prompt before each bet. Default \code{TRUE}.}

\item{today_only}{Restrict to matches on today.}

\item{target_date}{Restrict to matches on a specific \code{Date}.}

\item{headless}{Run Chrome headlessly. Default \code{TRUE}.}

\item{root}{Data root.}

\item{dual_write_csv}{Mirror placed bets to legacy CSV ledgers.
Default \code{TRUE}.}
}
\value{
Tibble of per-bet results with at minimum columns: \code{sport},
  \code{country}, \code{sex}, \code{match_date}, \code{home_team},
  \code{away_team}, \code{market}, \code{outcome}, \code{odds},
  \code{bet_amount}, \code{status}.
}
\description{
Top-level orchestrator for the placer. Reads recommendations from Parquet,
dedups against the ledger, optionally logs into Lengjan, and walks each
pending bet through the place-or-reject decision tree.
}
\details{
Returns one row per bet attempted (including those skipped or errored).
Status values: "placed", "rejected_p3", "rejected_p4", "dry_run", "error",
"no_match_id", "skipped_by_user".
}
