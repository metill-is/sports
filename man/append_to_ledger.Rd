% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/placer-ledger.R
\name{append_to_ledger}
\alias{append_to_ledger}
\title{Append a placed-bet row to the ledger.}
\usage{
append_to_ledger(
  bet_row,
  root,
  dual_write_csv = FALSE,
  legacy_root = normalizePath(file.path(root, "..", "_legacy", "sports"), mustWork =
    FALSE)
)
}
\arguments{
\item{bet_row}{Single-row tibble matching `schemas()$ledger`.}

\item{root}{Data root for the Parquet ledger.}

\item{dual_write_csv}{Default `FALSE` (Plan 6 cutover from Plan 5's
dual-write). Set `TRUE` only for opt-in regression-testing -- Parquet
is the canonical store from Plan 6 onward.}

\item{legacy_root}{Root path for legacy CSV writes (default
`<root>/../_legacy/sports`).}
}
\value{
invisible(NULL)
}
\description{
Writes to `data/decisions/ledger/` Parquet by reading the existing
partition, row-binding the new row, and calling [write_table()] — preserving
all existing rows. When `dual_write_csv = TRUE`, also appends to
`<legacy_root>/<sport>/<country>/history/bets_log.csv` for cutover safety.
}
\details{
This is the only writer to the ledger.
}
