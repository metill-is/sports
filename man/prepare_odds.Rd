% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/decide-odds.R
\name{prepare_odds}
\alias{prepare_odds}
\title{Read facts/odds for one (sport, country) and return latest-snapshot rows
for matches at or after `end_date`, no older than `max_age_hours`.}
\usage{
prepare_odds(
  league,
  sex,
  end_date = Sys.Date(),
  max_age_hours = 48,
  now = Sys.time(),
  root = here::here("data")
)
}
\arguments{
\item{league}{List with `sport` + `country`.}

\item{sex}{"male" or "female". Lengjan odds are sex-agnostic per
competition; the parameter is here for symmetry but does not filter.
Sex-aware handling happens at the kelly_joint stage where beliefs are
sex-keyed.}

\item{end_date}{Drop matches before this date. Default today.}

\item{max_age_hours}{Drop scrapes older than this (vs `now`).}

\item{now}{Reference timestamp for age filtering. Default `Sys.time()`.
Override in tests to avoid dependence on wall-clock time.}

\item{root}{Data root. Default `here::here("data")`.}
}
\value{
Tibble with (match_date, home_team, away_team, market, outcome,
  line, odds, scraped_at). Empty (typed) when no rows match.
}
\description{
Read facts/odds for one (sport, country) and return latest-snapshot rows
for matches at or after `end_date`, no older than `max_age_hours`.
}
