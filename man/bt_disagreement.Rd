% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/backtest-marketcal.R
\name{bt_disagreement}
\alias{bt_disagreement}
\title{Band the model-vs-line gap `p - q_market` and report who is right per band.}
\usage{
bt_disagreement(scored, by = NULL, breaks = c(-1, -0.1, -0.03, 0.03, 0.1, 1))
}
\arguments{
\item{scored}{Output of [bt_devig()].}

\item{by}{Optional grouping columns (the band is always added).}

\item{breaks}{Gap cut points. Default `c(-1, -.1, -.03, .03, .1, 1)`.}
}
\value{
`(<by..>, band, n, mean_p, mean_q, realised)`.
}
\description{
Where the model and the de-vigged line disagree most, whose probability tracks
the realised rate? If the market is right in a regime where they diverge, that
regime is a model weakness with an external second opinion.
}
