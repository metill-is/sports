% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/wc-simulate.R
\name{wc_forward_bracket}
\alias{wc_forward_bracket}
\title{Propagate the knockout bracket forward from a win matrix + R32 occupancy.}
\usage{
wc_forward_bracket(W, occ_a, occ_b, bracket, teams, pins = list())
}
\arguments{
\item{W}{`nt x nt` win matrix; `W[a, b]` = P(a beats b) in one neutral
knockout match (rows/cols indexed by team, 1..nt).}

\item{occ_a, occ_b}{`16 x nt` matrices; row i = the team-occupancy of slot
a / b of the i-th Round-of-32 match (in `bracket` order).}

\item{bracket}{The `structure$bracket` tibble (31 rows, ordered R32->Final).}

\item{teams}{Character vector of team names (length nt).}

\item{pins}{Optional named list `match_no -> team index` forcing a winner.}
}
\value{
List: `placement` (tibble team/round_name/probability, cumulative),
  `reach` (per-round occupancy), `winner` (per-match winner distribution).
}
\description{
Propagate the knockout bracket forward from a win matrix + R32 occupancy.
}
