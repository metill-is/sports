% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/schedule-active.R
\name{generate_active_competitions}
\alias{generate_active_competitions}
\title{Generate `config/active_competitions.json` from fixture data.}
\usage{
generate_active_competitions(
  leagues,
  lookahead_days = 7L,
  root = here::here("data"),
  out_path = here::here("config", "active_competitions.json")
)
}
\arguments{
\item{leagues}{Output of `load_leagues()` (or filtered subset).}

\item{lookahead_days}{Window size in days.}

\item{root}{Storage root.}

\item{out_path}{Path to write JSON.}
}
\value{
invisible(out_path).
}
\description{
Reads `data/facts/schedules/` (Parquet, partitioned by sport/country/sex/
season). A league is "active" if at least one fixture falls in
`[today, today + lookahead_days]`. Cold-start safe: missing schedule data
marks every league active to avoid silently skipping all scrapes.
}
