% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/decide-pipeline.R
\name{decide_league}
\alias{decide_league}
\title{Decide pipeline: beliefs + odds + bankroll -> candidates + recommendations.}
\usage{
decide_league(
  league_key = NULL,
  league = NULL,
  sex,
  run_date = Sys.Date(),
  root = here::here("data"),
  bankroll = NULL,
  write = TRUE
)
}
\arguments{
\item{league_key}{Key into load_leagues(). Mutually exclusive with `league`.}

\item{league}{Pre-loaded league list. Mutually exclusive with `league_key`.}

\item{sex}{"male" or "female".}

\item{run_date}{Stamped on every row's run_id partition. Default today.}

\item{root}{Data root. Default here::here("data").}

\item{bankroll}{Pre-loaded bankroll list (NULL = load_bankroll()).}

\item{write}{Write candidates + recommendations? Default TRUE.}
}
\value{
Tibble of recommendations (kept bets) invisibly.
}
\description{
Chains prepare_odds + kelly_joint + portfolio_optimise + compute_calibration,
then writes `candidates` (terminal-state audit log) and `recommendations`
(post-filter survivors) Parquet via write_table().
}
\details{
The `stage` column on candidates is the bet's terminal classification:
  - `kept`: passed all filters; appears in recommendations
  - `dropped_low_ev`: kelly_joint zeroed it (ev below ev_threshold)
  - `dropped_min_bet`: bet_amount fell below `betting$min_bet`
  - `dropped_market_off`: market disabled in `betting$markets` toggles
The pipeline does not emit per-step intermediate stages — the final stage
captures the decisive filter.
}
