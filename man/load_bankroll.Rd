% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/config.R
\name{load_bankroll}
\alias{load_bankroll}
\title{Load + validate the global bankroll config.}
\usage{
load_bankroll(
  path = here::here("config", "bankroll.yml"),
  ledger_root = here::here("data")
)
}
\arguments{
\item{path}{Path to bankroll.yml.}

\item{ledger_root}{Root directory for Parquet stores (parent of `decisions/ledger/`).}
}
\value{
List with `initial_pool`, `current_pool`, `daily_budget_frac`,
  `daily_budget_min_isk`.
}
\description{
Reads `config/bankroll.yml`. If `current_pool` is missing from the YAML
(the usual case), derives it as `initial_pool + sum(ledger$pnl[settled])`
so the bankroll evolves with realised PnL.
}
