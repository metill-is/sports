% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/backtest-metrics.R
\name{bt_skill}
\alias{bt_skill}
\title{Model-vs-market prediction-quality scores + Brier/log-loss skill.}
\usage{
bt_skill(scored, by = NULL, eps = 1e-06)
}
\arguments{
\item{scored}{Output of [bt_devig()] (`p`, `q_market`, `y`, `overround`).}

\item{by}{Optional grouping columns (e.g. `"market"`).}

\item{eps}{Log-loss clamp. Default 1e-6.}
}
\value{
One row (or one per `by` group) of model/market Brier + log-loss, their
  skill scores, `n`, `n_matches`, and mean `overround`.
}
\description{
On the de-vigged rows from [bt_devig()], scores the model `p` and the market
`q_market` against realised `y` with Brier + log-loss, and reports the skill
scores `1 - score_model/score_market` (> 0 == the model beats the margin-free
line). `n_matches` is the count of distinct fixtures -- the honest unit, since
outcome rows within a match are mechanically dependent.
}
