% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/round-cutoff.R
\name{completed_bd_rounds}
\alias{completed_bd_rounds}
\title{Enumerate completed top-division rounds with their cutoff dates.}
\usage{
completed_bd_rounds(results, season, top_division = "BD")
}
\arguments{
\item{results}{Tibble from `read_table("results")`. Must contain
`season`, `division`, `home_team`, `away_team`, `match_date`.}

\item{season}{Integer year of the season being analysed.}

\item{top_division}{Division code identifying the top flight. Default `"BD"`
(Besta deild for Icelandic football).}
}
\value{
tibble(`round` int, `cutoff_date` Date), ascending. Empty if none.
}
\description{
Walks `round_cutoff = 1, 2, ...` calling [compute_round_cutoff_date()] until
it returns `NULL` (round not yet complete). Returns one row per completed
round.
}
