% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/backtest-universe.R
\name{bt_load_universe}
\alias{bt_load_universe}
\title{Load the backtest bet universe from stored decisions.}
\usage{
bt_load_universe(
  root = here::here("data"),
  strategy = c("kept", "positive_ev", "all"),
  leagues = NULL,
  sex = NULL,
  from = NULL,
  to = NULL
)
}
\arguments{
\item{root}{Data root (default `here::here("data")`).}

\item{strategy}{One of `"kept"` (our actual picks; default), `"positive_ev"`
(every candidate with `ev > 0`), `"all"` (every candidate).}

\item{leagues}{Optional character vector of `sport` values to keep.}

\item{sex}{Optional character vector of `sex` values to keep.}

\item{from, to}{Optional `Date` or `YYYY-MM-DD` bounds on `run_date`.}
}
\value{
Tibble, one row per bet (see `bt_universe_cols()`); empty-with-columns
  if nothing matches.
}
\description{
Reads the leak-free `candidates` store (every evaluated market per decide
run, with `p`/`odds`/`ev`/`kelly_raw` frozen at decide-time) and left-joins
`recommendations` to attach the recorded effective Kelly fraction (`kelly`)
and stake (`bet_amount`) for bets that were actually kept. The `strategy`
argument selects the bet subset to evaluate -- the strategy *is* the filter.
}
