% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/backfill-final-positions.R
\name{build_round_final_positions}
\alias{build_round_final_positions}
\title{Build one round's final-position records from a paired (fit, prep).}
\usage{
build_round_final_positions(
  fit,
  prep,
  results,
  season_schedule,
  round_idx,
  cutoff_date,
  season,
  target_divs,
  generated_at,
  split_configs = NULL
)
}
\arguments{
\item{fit}{CmdStan fit trained on games <= `cutoff_date`.}

\item{prep}{`prepare_data()` output that built `fit`'s `stan_data` (pairing
is required for correct team-index -> name mapping).}

\item{results}{Played results (will be filtered to `<= cutoff_date`).}

\item{season_schedule}{Full-season schedule (this season).}

\item{round_idx}{Integer BD round.}

\item{cutoff_date}{Date of the round-`round_idx` completion.}

\item{season}{Integer season.}

\item{target_divs}{League division codes (no CUP).}

\item{generated_at}{ISO timestamp string stamped on every row.}

\item{split_configs}{Optional named list keyed by division code mapping to
`list(upper, lower)` split formats (from
`.football_iceland_division_split(sex)`). Divisions without an entry are
simulated flat. See the split-season section of
[simulate_league_season()].}
}
\value{
tibble(as_of, generated_at, round, season, division, team,
  placement, probability). Empty when no league team is covered.
}
\description{
For each league `target_div`, simulates the remaining season from the fit's
latest-round strengths (which, for a fit trained through this round, are the
as-of-round strengths) and ranks to season-end placements. Stamps each row
with the round's `as_of` cutoff date and the division's own games-played
round label.
}
