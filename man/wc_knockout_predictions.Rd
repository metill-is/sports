% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/wc-simulate.R
\name{wc_knockout_predictions}
\alias{wc_knockout_predictions}
\title{Predict the upcoming knockout fixtures (per-match cards).}
\usage{
wc_knockout_predictions(
  knockout_fixtures,
  sim_inputs_team,
  sim_inputs_scalar,
  structure,
  W
)
}
\arguments{
\item{knockout_fixtures}{Output of [wc_knockout_fixtures()].}

\item{sim_inputs_team, sim_inputs_scalar}{As for [simulate_world_cup()].}

\item{structure}{Output of [wc_structure()].}

\item{W}{The `nt x nt` neutral win matrix (`bracket_model$W`).}
}
\value{
Tibble in the [.wc_prediction_rows()] contract plus `group` (NA),
  `round`, and `p_advance`; or `NULL` if there are no knockout fixtures.
}
\description{
For each scheduled-but-unplayed knockout fixture (known teams), simulate the
match per posterior draw at a neutral venue and emit the same fixture-card
contract as the group predictions, plus `round` and `p_advance` (the
tie-resolved progression probability for the home team, taken from the neutral
win matrix `W` so the card and the bracket model agree exactly).
}
