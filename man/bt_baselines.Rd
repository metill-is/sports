% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/backtest-metrics.R
\name{bt_baselines}
\alias{bt_baselines}
\title{Compare strategies under both stake models.}
\usage{
bt_baselines(
  root = here::here("data"),
  results = NULL,
  initial_pool = NULL,
  flat_isk = 500,
  ...
)
}
\arguments{
\item{root}{Data root.}

\item{results}{Optional results tibble (read from `root` if NULL).}

\item{initial_pool}{Starting bankroll; default from `bankroll.yml`.}

\item{flat_isk}{Constant stake for the `flat_stake` strategy.}

\item{...}{Forwarded to [bt_load_universe()] (e.g. `leagues`, `from`, `to`).}
}
\value{
Tibble of metrics with `strategy` + `stake_model` columns.
}
\description{
Runs the engine for `our_picks` (kept), `all_positive_ev`, `flat_stake`
(kept bets at a constant ISK stake -- isolates selection from sizing), and
`favourites` (lowest-odds moneyline outcome per match -- naive baseline),
under both rolling and fixed stake models, and returns a tidy comparison.
}
