% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/model-fit.R
\name{fit_model}
\alias{fit_model}
\title{Fit a Stan model and return the result object.}
\usage{
fit_model(
  stan_data,
  stan_model_path,
  method = c("sample", "pathfinder", "variational"),
  chains = 4L,
  parallel_chains = chains,
  iter_warmup = 1000L,
  iter_sampling = 1000L,
  adapt_delta = 0.95,
  max_treedepth = 10L,
  num_paths = 4L,
  draws = 4000L,
  seed = NULL,
  init = 0,
  show_progress = TRUE,
  check_diagnostics = TRUE,
  max_divergent_frac = 0.01,
  max_rhat = 1.05,
  min_ess_bulk = 100
)
}
\arguments{
\item{stan_data}{Named list matching the model's `data {}` block.}

\item{stan_model_path}{Absolute path to a `.stan` file.}

\item{method}{"sample" (default), "pathfinder", or "variational".}

\item{chains}{Number of MCMC chains (MCMC only).}

\item{parallel_chains}{Number of chains to run in parallel.}

\item{iter_warmup, iter_sampling}{Iteration counts (MCMC only).}

\item{adapt_delta}{Target acceptance probability during warmup (MCMC only).
Default `0.95` (raised from Stan's stock 0.8 after the 2026-05-17 audit:
the football iceland model's funnel-shaped tails — driven by Mjólkurbikar
blowouts between top-flight and 4th-tier teams — produced 7% divergent
transitions at 0.8, tripping `check_stan_diagnostics()`). Higher = smaller
leapfrog stepsize, fewer divergences, slower warmup.}

\item{max_treedepth}{Maximum NUTS tree depth (MCMC only). Default `10`
(Stan stock).}

\item{num_paths}{Number of Pathfinder paths.}

\item{draws}{Number of draws for approximate methods.}

\item{seed}{Integer seed for reproducibility. NULL = cmdstanr default.}

\item{init}{Initial values passed to cmdstanr. Default 0 matches legacy.}

\item{show_progress}{Print cmdstanr progress bar? Default TRUE.}

\item{check_diagnostics}{If `TRUE` (default), call
[check_stan_diagnostics()] after sampling and abort with a clear
diagnostic on divergent transitions, R-hat, or ESS problems. Set to
`FALSE` only for explicit pre-flight model exploration where you
know the fit will be poor.}

\item{max_divergent_frac}{Maximum allowed fraction of post-warmup
iterations that diverged. Default `0.01` (1%).}

\item{max_rhat}{Maximum allowed R-hat on any monitored parameter.
Default `1.05`.}

\item{min_ess_bulk}{Minimum allowed bulk ESS on any monitored parameter.
Default `100`.}
}
\value{
CmdStanMCMC (sample) or CmdStanGQ (pathfinder / variational).
}
\description{
Thin wrapper over `cmdstanr::cmdstan_model()$sample()` (or `$pathfinder()`,
`$variational()`). Unlike the legacy counterpart, this does **not** write
to disk -- callers handle `save_object()`.
}
\details{
For approximate methods, `generate_quantities()` is called automatically
to produce predictive draws so downstream code (extract_posteriors) works
unchanged.
}
