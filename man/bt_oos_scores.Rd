% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/backtest-walkforward.R
\name{bt_oos_scores}
\alias{bt_oos_scores}
\title{Out-of-sample probability scores: Brier and log-loss over frozen as-of `p`.}
\usage{
bt_oos_scores(settled, eps = 1e-06)
}
\arguments{
\item{settled}{Tibble with numeric `p` and logical `win`.}

\item{eps}{Clamp bound. Default `1e-6`.}
}
\value{
One-row tibble `(n, brier, log_loss)`; `n = 0` -> NA scores.
}
\description{
The PRIMARY walk-forward verdict. Rows with `NA` win (bets that never
settled within the horizon) are dropped before scoring. `p` is clamped to
`[eps, 1-eps]` so a degenerate 0/1 forecast cannot send log-loss to Inf.
}
