% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/backtest-walkforward.R
\name{bt_walkforward_reuse}
\alias{bt_walkforward_reuse}
\title{Walk-forward in REUSE mode over the saved extract fit_dates (no Stan).}
\usage{
bt_walkforward_reuse(
  sex,
  season = NULL,
  horizon_days = 14L,
  root = here::here("data")
)
}
\arguments{
\item{sex}{"male" or "female".}

\item{season}{Optional integer year; filters the fit_dates to that season.}

\item{horizon_days}{OOS window cap (tail for the final cutoff). Default 14.}

\item{root}{Data root holding `beliefs/extracts/` + `facts/`.}
}
\value{
The [bt_walkforward()] list, or `NULL` if the cell has no saved
  extracts (e.g. an off-season sex).
}
\description{
Thin orchestration shared by the CLI (`--reuse`) and the season report:
enumerates the saved `predicted_matches` fit_dates for football_iceland/`sex`
(optionally one season), loads `results` + `odds`, and runs [bt_walkforward()]
with [bt_wf_extract_decide()]. A whole season scores in seconds.
}
