% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/placer-login.R
\name{chromote_login}
\alias{chromote_login}
\title{Create an authenticated Lengjan browser session}
\usage{
chromote_login(headless = TRUE, timeout_s = 30)
}
\arguments{
\item{headless}{Run headless? Default `TRUE` for CI; pass `FALSE` to watch
the browser click through.}

\item{timeout_s}{Seconds to wait after navigating / submitting (default 30;
currently used as per-step wait budget).}
}
\value{
A `ChromoteSession` object, logged in to Lengjan.
}
\description{
Establishes a Chromote session, navigates to games.lotto.is, fills
credentials from the `LENGJAN_USER` / `LENGJAN_PASS` environment variables,
and returns the live session ready for navigation.
}
\details{
Credentials are gated: missing or empty `LENGJAN_USER` / `LENGJAN_PASS`
produce a clean error before Chrome is opened.
}
