% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/auto-place.R
\name{run_auto_place}
\alias{run_auto_place}
\title{Run one unattended placement cycle (kill -> lock -> sync -> gate ->
daily-cap -> place), recording the outcome to the status store.}
\usage{
run_auto_place(
  root = here::here("data"),
  now = Sys.time(),
  sync_fn = sync_recs,
  place_fn = place_bets,
  bankroll_fn = function() load_bankroll(ledger_root = root),
  headless = TRUE
)
}
\arguments{
\item{root}{Data root.}

\item{now}{Current time.}

\item{sync_fn}{`function(repo_root) -> logical` (TRUE on clean sync).}

\item{place_fn}{`place_bets`-compatible function returning a status tibble.}

\item{bankroll_fn}{`function() -> load_bankroll()` list.}

\item{headless}{Passed through to `place_fn`. `TRUE` (default) is the robust
choice for unattended/launchd runs (no dependency on an active GUI session).
Set `FALSE` to watch a visible browser during a supervised run; the
human-paced `sample_delay()` in the placer applies either way.}
}
\value{
The recorded status list, invisibly.
}
\description{
`sync_fn`/`place_fn`/`bankroll_fn` are injected for testability; the shell
`scripts/auto_place.R` passes the real `sync_recs`, `place_bets`,
`load_bankroll`.
}
