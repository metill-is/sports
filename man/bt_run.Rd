% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/backtest-engine.R
\name{bt_run}
\alias{bt_run}
\title{Run a backtest: settle a bet universe and size stakes.}
\usage{
bt_run(
  universe,
  results,
  stake_rule = stake_rolling,
  initial_pool = NULL,
  match_date_window_days = 3L,
  ...
)
}
\arguments{
\item{universe}{Tibble from [bt_load_universe()].}

\item{results}{Results tibble (`read_table("results")`).}

\item{stake_rule}{[stake_rolling()] (default) or [stake_fixed()].}

\item{initial_pool}{Starting bankroll; default from `bankroll.yml`.}

\item{match_date_window_days}{Reschedule fallback window; default `3L`
(matches `settle_ledger()`).}

\item{...}{Forwarded to `stake_rule`.}
}
\value{
Per-bet tibble with `win, stake, pnl, pool_before, cum_pnl,
  pool_after`; `attr(., "pending")` is the count of unsettled bets dropped.
}
\description{
Win/loss is stake-independent, so the engine first resolves `win` per bet by
reusing [compute_settlement()] (per `(sport, country)` with the same
`tie_threshold` as `settle_ledger()`, so push/boundary semantics match the
live decider). Bets with no matching result are dropped (counted in the
`"pending"` attribute). The remaining settled bets pass to `stake_rule`,
which computes `stake` and `pnl`; the engine then accumulates `cum_pnl` and
`pool_after` in match-settlement order.
}
