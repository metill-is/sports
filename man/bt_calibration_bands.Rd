% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/backtest-metrics.R
\name{bt_calibration_bands}
\alias{bt_calibration_bands}
\title{Calibration reliability with Jeffreys intervals and a binomial consistency band.}
\usage{
bt_calibration_bands(settled, n_bins = 10, by = NULL, conf = 0.9)
}
\arguments{
\item{settled}{Per-bet tibble (`p`, logical `win`).}

\item{n_bins}{Probability bins. Default 10.}

\item{by}{Optional grouping columns.}

\item{conf}{Central mass for both intervals. Default 0.9.}
}
\value{
[bt_calibration()] columns plus `lo, hi, band_lo, band_hi`.
}
\description{
Extends [bt_calibration()]: `lo`/`hi` are the Jeffreys (Beta(k+.5, n-k+.5))
interval on each bin's realised frequency; `band_lo`/`band_hi` are the
consistency band -- the central interval of the realised frequency UNDER the
null that the bin is perfectly calibrated (Binomial(n, mean_p)/n). A bin whose
`realised_freq` sits outside `[band_lo, band_hi]` is miscalibrated beyond
sampling noise.
}
